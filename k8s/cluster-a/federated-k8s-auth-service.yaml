apiVersion: v1
kind: Service
metadata:
  name: federated-k8s-auth
  namespace: mariadb-auth-test
  labels:
    app: federated-k8s-auth
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: federated-k8s-auth
