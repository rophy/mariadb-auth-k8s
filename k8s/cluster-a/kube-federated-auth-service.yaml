apiVersion: v1
kind: Service
metadata:
  name: kube-federated-auth
  namespace: mariadb-auth-test
  labels:
    app: kube-federated-auth
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: kube-federated-auth
