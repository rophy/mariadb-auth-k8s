apiVersion: v1
kind: Service
metadata:
  name: token-validator-api
  namespace: mariadb-auth-test
  labels:
    app: token-validator-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: token-validator-api
